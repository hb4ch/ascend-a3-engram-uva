# 2026-09-17 工作总结

## 完成了什么

1. 验证 A3 支持设备通过映射地址读取 pinned DDR；先完成官方 Engram hash 与 gather 功能原型，再使用真实完整权重测试。
2. 从逐层单算子计时升级到集中部署形态：两层完整 INT8 表（183.11 GiB）同时驻留 DDR，完整 scale（22.89 GiB）驻留 HBM。统一输入为主存 token、mask、历史，终点为两层 BF16 embedding 在 HBM 就绪。
3. 增加 Ascend C 分块、向量 scale 广播、双输入/输出缓冲及下一 tile 预取；保留同分块串行版作为消融。
4. 修正 standalone ctypes 调用顺序：不能缓存 ACL stream 后绕过尚未提交的 PyTorch 主机任务。每次 raw hash/gather 发射前重新取得当前 stream，正常任务队列保持开启。
5. 报告补齐 CPU/NPU 分工、实际 H2D 搬运对象与字节数；提供 shape 折线图、阶段堆叠图、分位数和全部离群点。

## 性能结果：不能宣称流水化已成功解决瓶颈

以下为相同本轮测量条件的查表链路 E2E P50，单位 ms。

| [B,S] | 旧 Ascend C | 分块串行8行 | 流水8行 | 流水16行 | 上游 Triton UVA |
|---|---:|---:|---:|---:|---:|
| [1,1] | 0.1421 | 0.1457 | 0.1441 | 0.1488 | 0.2201 |
| [32,1] | 0.1889 | 0.1740 | 0.1771 | 0.1704 | 0.2369 |
| [128,1] | 0.4708 | 0.4219 | 0.4179 | 0.3961 | 0.2648 |
| [512,1] | 1.4493 | 1.3681 | 1.3558 | 1.3014 | 0.4771 |
| [8,512] | 9.5017 | 9.8590 | 9.7573 | 9.7093 | 2.3509 |

大 shape 的同分块流水收益约 1%，没有消除与 Triton 的差距。[512,1] 16 行版相比旧版约快 10%，[8,512] 反而略慢。小 shape 的 Python 发射成本也不同，不能由墙钟结果断言某种算子语言必然更快。双缓冲声明本身不证明实际存在充分的软流水重叠。

## 正确性证据与未解决事项

- 114 个 tile/模式/行数组合通过，包含零行、尾块、重复行号、边界行、DDR 更新和输出 guard。
- 11 shape × 7 路径 = 77 组；每组 50 次无打点 + 20 次带打点，共 5,390 次采样。每次之后在计时区间外与 CPU 真实权重 BF16 参考逐元素比较，全部一致。
- 原缓存 stream 调用能复现 producer 尚未提交造成的错误，重新获取 stream 后测试通过。这是测试调用边界缺陷，不是据此认定 MHC 或模型精度问题。
- 早期 [512,1] 曾出现 757 个元素差异，最大误差 5.125。新的发现提供线索，但没有独立证明该历史异常完全同源。保留 validation note，旧轮次不用于本轮公平比较。
- 所有结果仅覆盖 hash / gather / dequant；不代表完整 Engram projection、gate、卷积或模型精度验证。

## 各路径真实分工

- **CPU 收集 / NPU 解量化**：CPU 官方 hash → NumPy 从 DDR 收集 INT8 到连续 pinned buffer → 显式 H2D 复制 INT8 与行号 → NPU 从 HBM 收集 scale → Triton 反量化。两层每 token 复制 12,672 字节，不去重。
- **CPU 收集 / CPU 解量化**：CPU hash、收集 INT8 与 host scale、反量化到 BF16 → H2D 复制 BF16（两层每 token 24,576 字节）。不是完全没有搬运。
- **UVA**：token/mask H2D → NPU hash → kernel 直接从映射 DDR 读取 INT8，用 HBM scale 反量化 → BF16 HBM。无 CPU 收集 INT8 中间缓冲区。

CPU gather 为 NumPy 参考实现，不是优化 CPU 实现的性能上限。完整表装载不计入 E2E，所有路径统一保留完整 HBM scale。固定查询预热、后台权重复制、未绑定 NUMA 均限制结论外推。

## 下一步

优先用 profiler 和编译产物确认 DDR 随机读取、逐行 MTE 发射与 scalar 同步的开销，比较 Triton 实际生成的执行结构。再试更有效的请求并行及正式 PyTorch NPU 算子队列封装。保持同 shape、同数据路径与逐样本正确性检查；不要通过取消同步或缩小表容量制造性能收益。

## 入口

- [完整交互报告](benchmark/report-pipeline.html)
- [流水化实现与验证说明](benchmark/PIPELINE.md)
- [复现步骤](benchmark/README.md)
- [原始逐样本与阶段数据](benchmark/pipeline-e2e-results.json)
